<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FinApp5.Views.VerAbonos"
             Title="Ver Abonos">
    <ContentPage.Content>
        
        <ScrollView>
            
            <StackLayout Margin="10">
                <Button x:Name="btnVolver" Text="Volver a creditos" Clicked="btnVolver_Clicked" Margin="5"/>
                <Label Text="FECHA                             |      VALOR      |                   TIPO" />
                <ListView x:Name="lstMovimientos" Margin="10">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Row="0" Grid.Column="0" Text="{Binding FechaHoraReg, StringFormat='{0:dd/MM/yyyy hh:mm:ss tt}'}"  TextColor="Black"/>
                                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding ValorMovto, StringFormat='$ {0}'}"  TextColor="Black" HorizontalTextAlignment="Center" />
                                    <Label Grid.Row="0" Grid.Column="2" Text="{Binding strCodTipMov}" TextColor="Black" HorizontalTextAlignment="Center" />
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Label Text="Cantidad Microseguros: " x:Name="lblCantidadMicroseguros" />
                <Label Text="Total Microseguros: " x:Name="lblTotalMicroseguros" />
                <Label Text="Cantidad Abonos: " x:Name="lblCantidadAbonos" />
                <Label Text="Total Abonos: " x:Name="lblTotalAbonos" />
                <Label Text="Saldo Actual: " x:Name="lblSaldoActual" />
                
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>